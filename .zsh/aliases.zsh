# Directory tree navigation
alias -g ..='cd ..'
alias -g ...='cd ../..'

# Shortcuts
alias cl=clear
alias -g g=git
alias ll='ls -lA'
alias -g s=sudo
alias -g v=vim
alias yao=yaourt

# Files by suffix
alias -s c=$EDITOR
alias -s com=$BROWSER
alias -s h=$EDITOR
alias -s html=$BROWSER
alias -s java=$EDITOR
alias -s jpg=feh
alias -s js=$EDITOR
alias -s org=$BROWSER
alias -s png=feh
alias -s py=$EDITOR
alias -s PKGBUILD=$EDITOR

# Useful
alias -g G='| grep'
alias -g LC='| wc -l'
alias -g DN='/dev/null'
alias -g L='| less'
alias -g NUL='> /dev/null 2>&1'
