set nocompatible

set filetype=man

" 8 spaces per tab like `man` does
set tabstop=8
" Don't ask for saving changes
set nomodified
" Disable displaying of special symbols
set nolist
" Disable line numbers
set nonumber
" Not modifiable
set nomodifiable

nmap q :q!<CR>
