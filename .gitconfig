[user]
	name = Alejandro Gómez
	email = alejandroogomez@gmail.com
[core]
	editor = vim
[alias]
    a = add
    ap = add -p
    b = branch -va
    ci = commit
    ca = commit --amend -C HEAD
    cm = commit -m
    co = checkout 
    d = diff --word-diff
    l = log --graph --decorate --no-merges --oneline
    lp = log --graph --decorate --pretty=short -p --word-diff
    m = merge
    ph = push
    pl = pull --rebase
    rm = rm --cached
    publish = push --set-upstream
    s = status -sb
    st = stash
    stapp = stash apply
    stpop = stash pop
    stdis = stash discard
    timeline = log --graph --decorate --branches --oneline
    discard = reset HEAD --hard
[color]
	ui = true
[merge]
    tool = vimdiff
[push]
    default = tracking
[branch]
    autosetuprebase = always
